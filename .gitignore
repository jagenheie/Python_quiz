# Python cache
__pycache__/
*.pyc

# PyInstaller build folders
dist/
build/
*.spec

# Local high score (user-specific, don't share)
high_score.json

# The compiled exe (keep this in Releases, not in the repo)
py_quiz_v1.3.exe